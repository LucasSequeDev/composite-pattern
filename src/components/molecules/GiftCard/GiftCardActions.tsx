interface GiftCardActionsProps {
    children: React.ReactNode
}

export const GiftCardActions = ({ children }: GiftCardActionsProps) => {
  return (
        <>
            { children }
        </>
    )
}
